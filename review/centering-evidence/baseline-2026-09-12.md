# Full-suite baseline — 2026-09-12 (revision E)

This is the fresh REQ-039 baseline taken before the REQ-041/REQ-042 spike. It
uses the signed/default test path and the only booted simulator selected by
the environment check: iPhone 17 Pro, iOS 26.5, UDID
`EB1F0EB1-9B40-4FDA-B8D3-AEEF76909C86` (arm64, OS build `23F77`). The test
environment reports macOS `26.5.1`.

## Command and storage

The run was started from the `opus-card-centering-implementation` worktree:

```sh
env DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test \
  -project TradingCardScanner.xcodeproj \
  -scheme TradingCardScanner \
  -configuration Debug \
  -destination 'platform=iOS Simulator,id=EB1F0EB1-9B40-4FDA-B8D3-AEEF76909C86' \
  -derivedDataPath '/Volumes/Keller Family Photos/June 10 2026 dump (move)/AdditionalStorage/TradingCardScannerMVP_fixed_v4/DerivedData/revision-e-full-suite-2026-09-12' \
  -resultBundlePath '/Volumes/Keller Family Photos/June 10 2026 dump (move)/AdditionalStorage/TradingCardScannerMVP_fixed_v4/Artifacts/revision-e-full-suite-2026-09-12.xcresult' \
  -collect-test-diagnostics never
```

No `CODE_SIGNING_ALLOWED=NO` override was supplied. The result bundle is
retained at the SSD path above. Its machine-readable summary was read with:

```sh
env DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcrun xcresulttool get test-results summary \
  --path '/Volumes/Keller Family Photos/June 10 2026 dump (move)/AdditionalStorage/TradingCardScannerMVP_fixed_v4/Artifacts/revision-e-full-suite-2026-09-12.xcresult'
```

## Result

```text
1088 total test cases
1078 passed
1 skipped
9 failed test cases
116 assertion failures reported in the xcodebuild console
Test session: 1037.664 seconds
Overall result-bundle interval: approximately 1092.439 seconds
```

The result is `TEST FAILED`. The distinction between nine failed test cases
and 116 assertion failures is intentional: several test cases report multiple
assertions.

### Failed test cases

Eight failed test cases are centering evidence:

- `CardCenteringGroundTruthTests.testAnalyzerMatchesHoldoutGroundTruthThroughProductionEntryPoint()`
  — current rederived-GT ratio failures for IMG_0349, IMG_0783, and IMG_0351.
- `CardCenteringGroundTruthTests.testFrontArtWindowIsNotReplacedByTheFullCardPrintedBorder()`
  — IMG_0780 ratio mismatch.
- `CardCenteringGroundTruthTests.testL1PublicPipelineReportsEveryFixtureAndNeverConfidentlyWrong()`
  — current real-fixture outer/inner geometry, ratio, rectification, and
  reference-role failures, including the wrong `artWindow` result for a back.
- `CardCenteringGroundTruthTests.testPortraitFrontArtWindowUsesTheGradeableInnerReference()`
  — IMG_0348 ratio mismatch.
- `CardCenteringInvariantTests.testINV4HorizontalMirrorComplementsLeftRightOnly()`
  — 1.4 pp drift versus a 0.5 pp limit.
- `CardCenteringInvariantTests.testINV5QuarterTurnsMapThePerSideRatios()`
  — 0.7 pp at 90°, 1.4 pp at 180°, and 1.1 pp at 270°, each versus a
  0.5 pp limit.
- `CardCenteringInvariantTests.testINV7UniformScaleDoesNotChangeRatios()`
  — 1.4 pp drift at 0.6× versus a 1.0 pp limit.
- `CardCenteringInvariantTests.testREQ022AnalysisPerformanceOverAllFixtures()`
  — measured 2.744 s median and 2.950 s max versus 0.80/1.50 s.

The ninth failed test case is:

- `MagicTreatmentTests.testTreatmentQualifiedDisplayLabelsCoverAtLeastTwentyDualFinishFixtures()`
  — the pre-existing `Nonfoil · Plastic` label leak. The centering diff does
  not modify Magic treatment sources, so this is not attributed to the
  centering work.

There were no Keychain `-34018` failures in this normally signed baseline.

## Passing centering evidence in the same run

- `CardCenteringAnalyzerTests`: 21/21 passed, including sideways-card and
  skewed-card regressions.
- `CardCenteringGroundTruthTests.testMaskFailureDeclinesWithoutAnInnerReference()`:
  passed; IMG_0782 remains declined with no exposed ratio.
- INV-2, INV-3, INV-6, INV-8, INV-9, and INV-10 passed.
- Manual recovery and declined-measurement/no-ratio safeguards passed.
- The retained E0, E-C, E-A, E-B, E-D, and E-E diagnostic tests passed in this
  run; they remain diagnostic evidence and do not close the failed L1 gates.

This baseline confirms the revision-E decision: the project and simulator
path are usable, the geometry/product scaffolding and safety fallback are
working, and the remaining work is the bounded semantic candidate/selection
spike. It does not authorize reopening the closed sampling experiment class.

